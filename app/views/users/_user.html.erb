<li>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "kasuj", user, method: :delete,
                                  data: { confirm: "Na pewno chcesz skasować?" } %>
  <% end %>

</li>